const { PATCH } = require('./apps/command/app/api/v1/nodlrs/[wuid]/route.js'); // Actually, since it's TS, running it in node requires transpile. 
// I'll just write a quick fetch test since Next.js isn't running on dev, but it IS running on port 3001! 
// Wait, port 3001 is running the OLD build, so the route won't be there until I rebuild.
