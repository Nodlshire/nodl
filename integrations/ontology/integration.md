# Ontology Integration

This integration connects Wnode to Ontology.

## Status
Integrated — Activation Coming Online

## Overview
Ontology integration allows Wnode operators to seamlessly connect with Ontology.

## SDK Usage
```ts
import { ontology } from "./sdk";

ontology.ping();
```
