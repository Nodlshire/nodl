export class PeaqXcmRoutingTemplate {
  initiateTransfer(transferDetails: any) {
    // sdk.xcm.initiate_transfer
  }

  queryDeliveryStatus(messageId: string) {
    // sdk.xcm.query_delivery_status
  }
}
